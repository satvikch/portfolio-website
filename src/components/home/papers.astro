---
import Button from "../button.astro";
import Paper from "../paper.astro";
import papers from "../../collections/papers.json";
---

<section class="max-w-4xl mx-auto px-7 py-5 lg:px-0">
  <h2
    class="text-2xl font-bold leading-10 tracking-tight text-neutral-900 dark:text-neutral-100"
  >
    My Papers
  </h2>
  <p class="mb-6 text-base text-neutral-600 dark:text-neutral-400">
    Here are some of my recent reseach papers. I'm always working on something
    new, so check back often!
  </p>
  <div
    class="grid items-stretch w-full sm:grid-cols-2 md:grid-cols-3 gap-7 mt-7"
  >
    {
      papers.map((paper) => {
        return (
          <Paper
            name={paper.name}
            description={paper.description}
            published={paper.published}
            image={paper.image}
            url={paper.url}
          />
        );
      })
    }
  </div>

  <div class="flex items-center justify-center w-full py-5">
    <Button
      text="View All My Papers 🔗"
      link="https://scholar.google.com/citations?user=l5lTq8cAAAAJ&hl=en&oi=ao"
    />
  </div>
</section>
